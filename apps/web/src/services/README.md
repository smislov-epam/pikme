# Services

Keep side-effectful code here (BGG API integration, persistence adapters, scoring helpers).

Suggested modules (to be implemented):
- `bggApi.ts` (collection + thing endpoints, 202 polling)
- `storage.ts` (Dexie/localStorage wrappers)
- `scoring.ts` (pure scoring functions)
