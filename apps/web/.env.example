# =============================================================================
# Firebase Configuration (REQ-100)
# =============================================================================
# Copy this file to .env.local and fill in values from your Firebase Console.
# All Firebase features are DISABLED by default.

# Feature flag: set to 1 to enable Firebase integration
VITE_FEATURE_FIREBASE=1

# Firebase project configuration (from Firebase Console → Project Settings)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=

# =============================================================================
# Emulator Configuration (for local development)
# =============================================================================
# Set to 1 to connect to Firebase Emulator Suite instead of production
VITE_FIREBASE_USE_EMULATORS=1

# Emulator host (usually localhost)
VITE_FIREBASE_EMULATOR_HOST=localhost

# Emulator ports (match firebase.json settings)
VITE_FIREBASE_AUTH_EMULATOR_PORT=9098
VITE_FIREBASE_FIRESTORE_EMULATOR_PORT=8081
VITE_FIREBASE_FUNCTIONS_EMULATOR_PORT=5001
